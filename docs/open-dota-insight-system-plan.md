# OpenDota Insight System implementation reference

The approved implementation plan is kept at the repository root in `PLAN.md`. This file is the stable documentation entry point for tooling that expects the plan under `docs/`.

