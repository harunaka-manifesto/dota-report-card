# Free DNA — Death Context Rejection

## Decision

**DROP_DEATH_CONTEXT**

The complete frozen 32-profile × 30-match panel was available, the registered
analysis ran, and the candidate failed the predeclared adjusted residual-IQR
gate: `0.091519 < 0.10`.

## Evidence

- complete panel: 960 / 960;
- core field completeness: 100%;
- teamfight semantics structural gate: PASS, semantic confidence LIKELY;
- supported profiles: 32 / 32;
- adjusted residual IQR: 0.091519 — FAIL;
- common-law dominant direction: 0.53125 — PASS;
- hero direction retention: 0.875 — PASS;
- role/outcome/game-state/patch direction retention: 0.96875 — PASS;
- median absolute attenuation: -0.015198 — PASS;
- N25 stability: PASS, split-half Spearman 0.512097;
- N30 stability: PASS, split-half Spearman 0.509530.

The rejection is scientific, not a collection failure. It does not claim that
the provider transport was impossible: the final-10 campaign completed all ten
remaining details on ten first-attempt HTTP 200 responses. It means the
registered personalized residual distribution was not wide enough to meet the
frozen gate.

## Interpretation boundary

This does not support a Death Context Finding or production routing. The result
must not be relabeled as death risk, KDA, aggression, skill, intent, or causal
positioning advice. The rejected ahead-state, pre-objective, game-phase, and
isolation branches remain rejected and are not fallback winners.

## Method and integrity

No panel reselection, replacement, adaptive top-up, replay parse, STRATZ call,
Steam call, holdout evaluation, fresh sealed validation, threshold change,
artifact regeneration, public contract change, or production deployment
occurred. The complete output remains research-only in the ignored local
corpus and diagnostics.

The existing research helper required a narrow implementation correction before
the accepted run: per-match rather than all-panel match exclusion, plus the
documented `(rate, mode)` return shape. This restored the registered method and
did not change the method, gates, thresholds, or production analytical behavior.

## Next

STOP. Do not redesign Death Context or create a calibration prompt from this
pilot. See the [final completion receipt](free-dna-death-context-final-completion-2026-08-29.md)
for the full transport, corpus, stability, and staging record.
